# =============================================================================
# SecBoard Community Edition — typical commands / типові команди
# =============================================================================
# EN: Open Core (AGPL v3) + Dual License. See LICENSING.md.
# UK: Open Core (AGPL v3) + Dual License. Див. LICENSING.md.
# EN: Run all Django/Celery commands from the project root (directory with manage.py).
# UK: Усі команди Django/Celery виконуйте з кореня проєкту (каталог із manage.py).
# =============================================================================

# -----------------------------------------------------------------------------
# First-time setup / Перше налаштування
# -----------------------------------------------------------------------------
#   cp .env.example .env
#   nano .env
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install --upgrade pip
#   pip install -r requirements.txt
#   python manage.py migrate
#   python manage.py createsuperuser
#   python manage.py collectstatic --noinput
#   python manage.py check

# -----------------------------------------------------------------------------
# Virtual environment / Віртуальне середовище
# -----------------------------------------------------------------------------
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#   deactivate

# -----------------------------------------------------------------------------
# Django — database / База даних
# -----------------------------------------------------------------------------
#   python manage.py makemigrations
#   python manage.py migrate
#   python manage.py showmigrations

# -----------------------------------------------------------------------------
# Django — run, check, superuser
# -----------------------------------------------------------------------------
#   python manage.py runserver
#   python manage.py check
#   python manage.py createsuperuser

# -----------------------------------------------------------------------------
# Django — i18n / Переклади
# -----------------------------------------------------------------------------
#   python manage.py makemessages -l en -l uk -l ru --ignore=venv
#   python manage.py compilemessages --ignore=venv

# -----------------------------------------------------------------------------
# Django — static files / Статичні файли
# -----------------------------------------------------------------------------
#   python manage.py collectstatic --noinput

# -----------------------------------------------------------------------------
# Celery / Фонові задачі
# -----------------------------------------------------------------------------
#   celery -A SecBoard worker -l info
#   celery -A SecBoard beat -l info

# -----------------------------------------------------------------------------
# SecBoard Community Edition — periodic tasks
# -----------------------------------------------------------------------------
#   python manage.py setup_periodic_tasks
#   python manage.py setup_api_sync_tasks
#   python manage.py setup_all_periodic_tasks

# -----------------------------------------------------------------------------
# License (Community / Enterprise) / Ліцензія
# -----------------------------------------------------------------------------
#   python manage.py get_hardware_id
#   # Free community key (up to 100 users): https://secboard.online
#   python manage.py activate_license YOUR_COMMUNITY_OR_ENTERPRISE_KEY
#   python manage.py check_license
#   python manage.py test_license_server

# -----------------------------------------------------------------------------
# Production service (example) / Продакшен-сервіс
# -----------------------------------------------------------------------------
#   sudo cp deploy/secboard_base.service.example /etc/systemd/system/secboard_base.service
#   sudo systemctl daemon-reload
#   sudo systemctl enable --now secboard_base.service

# -----------------------------------------------------------------------------
# Release archive / Архів релізу
# -----------------------------------------------------------------------------
#   ./scripts/build_release.sh
